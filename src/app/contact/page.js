import ContactContent from "./ContactContent";

export const metadata = {
  title: "Contact | OMKAR CREATIONS",
  description: "Get in touch with Omkar Creations for collaborations, project files, or inquiries.",
};

export default function ContactPage() {
  return <ContactContent />;
}
